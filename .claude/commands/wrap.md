---
description: End-of-session wrap — update docs/JOURNAL.md and CLAUDE.md
---

Wrap up the current work session:

1. Draft a dated entry for `docs/JOURNAL.md`, inserted at the top (below the
   header/instructions), following the template already in that file:
   **Did / Decided / Result-numbers / Open-broken / Next** (one single next
   step). Keep it terse — memory aid, not documentation. Include run-folder
   names and key metrics if a run happened.
2. Review whether any decision, convention, constraint, or file-structure
   choice changed this session. If so, draft the matching `CLAUDE.md` edit.
3. Show both drafts to the user for confirmation before applying.
4. After applying, check `git status` and remind the user about uncommitted
   changes (do not commit unless asked).
